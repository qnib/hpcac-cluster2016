{
    "bootstrap_expect": 2,
    "server": true,
    "datacenter": "vagrant",
    "data_dir": "/var/consul/",
    "log_level": "INFO",
    "enable_syslog": false,
    "ui_dir": "/opt/consul-webui/",
    "client_addr": "0.0.0.0",
    "start_join": [{% for host in groups['p_hosts'] %}{{"\n        "}}"{{ hostvars[host]['ansible_default_ipv4']['address'] }}"{% if not loop.last %},{% endif %}{% endfor %}{{"\n    "}}],
    "advertise_addr": "{{ ansible_default_ipv4.address }}",
    "advertise_addr_wan": "",
    "recursor": "8.8.8.8",
    "retry_max_wan": 3
}
